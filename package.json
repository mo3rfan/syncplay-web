{
  "name": "SyncPlay-js-client",
  "version": "0.0.1",
  "scripts": {
    "flow": "flow; test $? -eq 0 -o $? -eq 2"
  },
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-plugin-transform-flow-strip-types": "^6.18.0",
    "babel-plugin-transform-remove-console": "^6.8.0",
    "flow-bin": "^0.36.0"
  }
}
